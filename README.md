# A simple starter for Threejs

This project contains a single spinning cube and an ambient light. 

## How to run

1. Navigate to root folder
2. run `npm i` in terminal
3. run `npm run start` to start a dev server